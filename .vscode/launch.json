{
    "configurations": [
        {
            "name": "smallweb up",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/main.go",
            "args": [
                "up",
            ]
        },
        {
            "name": "smallweb dump",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/main.go",
            "args": [
                "dump",
                "${input:base}"
            ]
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "base",
            "description": "Enter the base name",
            "default": "localhost"
        }
    ]
}
