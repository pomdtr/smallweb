{
    "configurations": [
        {
            "name": "smallweb up",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/main.go",
            "args": [
                "up",
            ]
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "app",
            "description": "Enter the app name",
        }
    ]
}
